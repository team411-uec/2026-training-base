// =============================================================
// 描画層 (render.ts)
// -------------------------------------------------------------
// 「データ(状態)を受け取って、画面(DOM)に表示する」だけを担当する層です。
// おみくじを引くロジックや、ボタンのクリック判定などは書きません。
//   ・引くロジック  → omikuji.ts（データ層）
//   ・ボタンとの配線 → main.ts（イベント層）
//
// ★この層が、研修の主役「TypeScript で HTML を操作する」の中心です。
// =============================================================

import type { OmikujiResult } from "./omikuji";

// =============================================================
// ★★★ ステップ1（最初の課題）：この関数を実装しよう！ ★★★
// -------------------------------------------------------------
// 今の状態で「引く」ボタンを押すと、ブラウザの開発者ツール(F12)の
// Console には「引いた結果: 大吉」のように表示されます。
// つまり、おみくじ自体はちゃんと動いています。
//
// でも画面の文字は「ここに結果が出ます」のまま変わりません。
// なぜなら ↓ のこの関数の中身が空っぽだからです。
//
// この関数の中に「画面の文字を書き換える」処理を書いて、
// 引いた結果が画面に出るようにしてください。
//
// ＜ヒント＞
//  1. 結果を表示する要素を取得する。
//       index.html に id="result" の要素があります。
//       const el = document.getElementById("result");
//  2. その要素の文字を書き換える。
//       el.textContent = result;  // textContent で中の文字を変えられます
//  3. result が null のときは、結果がまだ無い状態です。
//       「ここに結果が出ます」などの初期メッセージを出すとよいです。
//       （リセットボタンを押したときも、main.ts から null が渡ってきます）
//
// ＜発展ヒント（やってみたい人向け）＞
//  ・el.dataset.result = result; のように data-result 属性を入れておくと、
//    あとで CSS の [data-result="大吉"] で「大吉だけ金色にする」等の演出ができます。
//  ・drawOmikuji() は箱が空のときにも null を返します。余裕があれば
//    「リセット直後」と「箱が空」を区別して別メッセージを出してみましょう。
// =============================================================
export const renderResult = (result: OmikujiResult | null): void => {
  // TODO（ステップ1）: ここに DOM 操作を書いて、画面に結果を表示しよう。
};

// =============================================================
// ★拡張ポイント（描画層）：ステップ2以降。必要になったら関数を足そう。
// -------------------------------------------------------------
// ・引いた履歴をリスト表示したい
//     → 履歴用の関数を足す。createElement で <li> を作り、
//       appendChild で <ul id="history"> に追加していく。
//       例:
//       export const renderHistory = (history: OmikujiResult[]): void => {
//         const list = document.getElementById("history");
//         // ... <li> を作って追加する ...
//       };
//
// ・残りくじ枚数を表示したい
//     → omikuji.ts に残数を読むメソッドを足したうえで、
//       ここに表示用の関数（例: renderRemaining）を足す。
// =============================================================
